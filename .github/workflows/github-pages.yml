name: Build and deploy Jekyll site to Github Pages and

on:
  push:
    branches:
      - master

jobs:
  github-pages:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v2
    - uses: helaili/jekyll-action@2.2.0
      with:
        token: ${{secrets.GITHUB_TOKEN}}
